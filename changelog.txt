1.4.0:
  - added support for putting the hat in the slot manually (helmet, jackolantern, etc unaffected)(permissions are the same as for the command)
  - changed the "no permission" message and "too large stack size" message in the default messages file slightly

1.4.3
  - Allowed the /hat command to be run while holding air to remove the current hat.

1.4.4
  - Allowed messages to be disabled by a boolean in messages.yml (except for the console message when using /hat)